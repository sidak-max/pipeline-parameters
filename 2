#app.py
def sum_list(numbers):
    return sum(numbers)


def count_negatives(numbers):
    count = 0

    for n in numbers:
        if n < 0:
            count += 1

    return count
    
#test_app.py
import pytest
from app import sum_list, count_negatives


@pytest.mark.parametrize("numbers, expected", [
    ([1, 2, 3], 6),
    ([5, 5], 10),
    ([], 0),
    ([-1, 2, -3], -2)
])
def test_sum_list(numbers, expected):
    assert sum_list(numbers) == expected


@pytest.mark.parametrize("numbers, expected", [
    ([-1, 2, -3], 2),
    ([1, 2, 3], 0),
    ([-5, -2, 4, -1], 3)
])
def test_count_negatives(numbers, expected):
    assert count_negatives(numbers) == expected
    
#Jenkinsfile
pipeline {
    agent any

    stages {

        stage('Checkout') {
            steps {
                git 'https://github.com/shivendra534/your-repository.git'
            }
        }

        stage('Install Dependencies') {
            steps {
                bat 'pip install -r requirements.txt'
            }
        }

        stage('Run Unit Tests (verbose)') {
            steps {
                bat 'pytest -v'
            }
        }
    }
}

#Number of tests

There are:

4 sum_list tests
+
3 count_negatives tests
=
7 tests

#Console Output will show approximately:

7 passed

The parametrized tests are labelled using their parameter values, for example:

test_sum_list[numbers0-6]
test_sum_list[numbers1-10]
...

If you add a wrong expected value:

([-1, -2], 5)

the corresponding test becomes:

FAILED

while the other valid cases remain:

PASSED
